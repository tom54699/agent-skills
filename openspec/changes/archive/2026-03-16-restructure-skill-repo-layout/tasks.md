## 1. OpenSpec Alignment

- [x] 1.1 補齊 `restructure-skill-repo-layout` 的 proposal、design、spec、tasks。

## 2. Repo Layout

- [x] 2.1 建立 `skills/.curated/` 與 `skills/.experimental/` 目錄結構。
- [x] 2.2 將 `laravel-api-docs` 搬移到 `skills/.curated/laravel-api-docs`。

## 3. Documentation

- [x] 3.1 更新 `README.md`，說明多 skill repo 結構與 repo-based 安裝方式。
- [x] 3.2 新增 `docs/install-skills.md`，集中說明 `curated` / `experimental` 與 `npx skills add` 安裝方式。

## 4. Reference Cleanup

- [x] 4.1 更新非 archive 的 `openspec/changes/**` 活引用，改指向 `skills/.curated/laravel-api-docs`。
- [x] 4.2 驗證 repo 內非 archive 範圍已無舊 skill 路徑殘留。
