## 1. Route List 容錯

- [ ] 1.1 在 `scan-routes.sh` 清理 `php artisan route:list --json` 的前導非 JSON 輸出。
- [ ] 1.2 在 `infer-candidates.sh` 清理 `php artisan route:list --json` 的前導非 JSON 輸出。

## 2. 驗證

- [ ] 2.1 執行 shell 語法檢查。
- [ ] 2.2 在存在 PHP startup warning 的專案確認候選推測可繼續執行。
